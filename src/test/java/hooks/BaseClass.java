package hooks;

import org.openqa.selenium.WebDriver;

import java.util.logging.LogManager;
import java.util.logging.Logger;

public class BaseClass {
    protected WebDriver driver;

    //public static Logger logger= LogManager.getLogManager(BaseClass.class);
    //public static org.slf4j.Logger log= LogManager.getLogManager(BaseClass.class);
}
